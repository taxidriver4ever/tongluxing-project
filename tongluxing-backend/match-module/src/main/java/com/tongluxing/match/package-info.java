/**
 * 同路行“发现同行”与路线匹配领域模块。
 *
 * <p>模块根据行程起终点、出发时间、途经点、车辆要求和剩余容量生成推荐，提供
 * 推荐列表、公开发现、附近行程、收藏、咨询和入队申请等能力。跨模块数据只能通过
 * {@code integration} 端口读取，匹配模块不直接操作行程或车队模块的数据表。</p>
 *
 * <p>推荐分数属于辅助排序结果，不代表入队成功。申请是否通过、车队成员关系以及
 * 聊天权限仍由 team-module 负责，避免推荐模块越过领域边界直接改变成员状态。</p>
 */
package com.tongluxing.match;
