# 同路行商家中心

Vue 3 + Vite 商家经营后台。当前首期闭环包括：统一账号登录、商家权限校验、经营总览、门店管理、优惠券发布/重提、商家资料和收款设置。

## 本地运行

```bash
npm install
npm run dev
```

默认地址为 `http://localhost:5175`，开发服务器把 `/api` 代理到 `http://127.0.0.1:18080`。可通过 `VITE_API_BASE_URL` 覆盖 API 地址。

商家后台与 App 共用手机号密码。账号必须已通过商家入驻审核，登录后才可进入工作台。
