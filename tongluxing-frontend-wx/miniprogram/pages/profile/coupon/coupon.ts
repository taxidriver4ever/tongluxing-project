import { mockCoupons } from "../../../data/mock-core"
Page({ data: { active: "available", tabs: [{ key: "available", label: "可使用" }, { key: "used", label: "已使用" }, { key: "expired", label: "已过期" }], coupons: mockCoupons, visible: mockCoupons.filter(item => item.status !== "used") }, back() { wx.navigateBack() }, change(event: WechatMiniprogram.TouchEvent) { const active = event.currentTarget.dataset.key; this.setData({ active, visible: active === "available" ? this.data.coupons.filter(item => item.status !== "used") : this.data.coupons.filter(item => item.status === active) }) }, detail(event: WechatMiniprogram.TouchEvent) { wx.navigateTo({ url: "/pages/profile/coupon-detail/coupon-detail?id=" + event.currentTarget.dataset.id }) } })
