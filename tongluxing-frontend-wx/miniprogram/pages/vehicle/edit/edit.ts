import { mockVehicles } from "../../../data/mock-core"
Page({ data: { editing: false, brand: "", model: "", plate: "", color: "", seats: 5, seatOptions: [2, 4, 5, 6, 7], isDefault: false }, onLoad(options: Record<string, string>) { if (!options.id) return; const item = mockVehicles.find(vehicle => vehicle.id === options.id); if (item) this.setData({ editing: true, brand: item.brand, model: item.model, plate: item.plate, color: item.color, seats: item.seats, isDefault: item.default }) }, input(event: WechatMiniprogram.Input) { this.setData({ [event.currentTarget.dataset.key]: event.detail.value }) }, seat(event: any) { this.setData({ seats: this.data.seatOptions[Number(event.detail.value)] }) }, toggle(event: WechatMiniprogram.SwitchChange) { this.setData({ isDefault: event.detail.value }) }, save() { if (!this.data.brand || !this.data.model || !this.data.plate) { wx.showToast({ title: "请填写完整车辆信息", icon: "none" }); return } wx.showToast({ title: "车辆已保存", icon: "success" }); setTimeout(() => wx.navigateBack(), 500) } })
