<view class="topbar" style="{{topbarStyle}}">
  <view wx:if="{{showBack}}" class="back-btn" bindtap="goBack">‹</view>
  <view class="city">{{title}}</view>
  <view class="search">{{placeholder}}</view>
  <view wx:if="{{action}}" class="icon-btn">{{action}}</view>
</view>
