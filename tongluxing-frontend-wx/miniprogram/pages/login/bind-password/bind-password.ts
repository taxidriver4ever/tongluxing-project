Page({ data: { password: "", confirm: "" }, goBack() { wx.navigateBack() }, passwordInput(event: WechatMiniprogram.Input) { this.setData({ password: event.detail.value }) }, confirmInput(event: WechatMiniprogram.Input) { this.setData({ confirm: event.detail.value }) }, submit() { if (this.data.password.length < 8) { wx.showToast({ title: "密码至少8位", icon: "none" }); return } if (this.data.password !== this.data.confirm) { wx.showToast({ title: "两次密码不一致", icon: "none" }); return } wx.showToast({ title: "绑定成功", icon: "success" }); setTimeout(() => wx.navigateTo({ url: "/pages/login/scan-invite/scan-invite" }), 500) } })
